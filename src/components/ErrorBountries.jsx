import React from 'react'

const ErrorBountries = ({
    children
}) => {
    return (
        <div>
            Network Error
            {children}
        </div>
    )
}

export default ErrorBountries